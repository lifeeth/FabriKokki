server {
	listen   80;
	{% if env.hostname %}
	server_name  {{ env.hostname }};
	{% endif %}

	access_log   {{ env.nginx.log_dir }}/localhost.access.log;

	location / {
		root   /var/www/nginx-default;
		index  index.html index.htm;
	}
}
